name: Test the PALINDROME portal

on:
   push

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test the PALINDROME portal
        run: | 
          echo '''${{ secrets.PORTAL_URL }}/${{ secrets.PORTAL_PASSWORD }}'''
